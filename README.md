# orientation.app

Change auth0 config for authentication
Orientation.App>orientation.app.spa>src>auth_config.json

Change MongodbConnection in MongoAccess.cs

Requirements to install npm packages
Node Js

To run client side
"npm run serve"
